{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--headless",
        "--executable-path", "/opt/pw-browsers/chromium",
        "--ignore-https-errors",
        "--isolated",
        "--no-sandbox"
      ]
    }
  }
}
