{
  "mcpServers": {
    "playwright": {
      "command": "sh",
      "args": [
        "-c",
        "exec npx -y @playwright/mcp@latest --browser chromium --executable-path /opt/pw-browsers/chromium-1194/chrome-linux/chrome --proxy-server=\"$HTTPS_PROXY\""
      ]
    }
  }
}
